function iniciar() {
    var inputElement = document.getElementById("numeroInput").value;
    var numero = inputElement.value;
    if (!isNaN(numero)) {
        // Divido los numeros en un array
        var digitosArray = numero.split('');
    }else if(){

    } else {
        alert("eso no es un numero papu");
    }
}